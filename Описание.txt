2) Для хранения данных в DWH, мною была выбрана схема, представленная в "Схема_DWH.drawio.png". Такой формат позволил избавиться от повторяющихся строк в таблице stream_module. Привязку я сделал сразу к course, таким образом, храня только модули, которые входят в состав своих курсов. Остальные таблицы остались без изменений.
К сожалению, я так и не получил уточнение о привязке между таблицами в исходной базе данных, по этому я выбрал отношение одного ко многим между таблицами (подробнее указано в схеме).

4) SQL-запрос для получения всей информации из DWH
select * 
from course
inner join stream on stream.course_id = course.id
inner join module on module.course_id = module.id
inner join module_lesson on module_lesson.module_id = module.id

P.S. Код не был до конца протестирован и у меня остались вопросы по информации из источника, по этому возможна доработка DAG